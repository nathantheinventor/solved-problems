#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    vector<int> nums;
    vector<bool> bits;
    int n, k;
    cin >> n >> k;
    for (int i = 0; i < n; i ++) {
        int x;
        cin >> x;
        nums.push_back(x);
    }

}
